/*const text = 'he';
var fs = require('fs');
const crashfontString = 'abcdefghijklmnopqrstuvwxyz0123456789.:! ';
var merge = require('merge-img');
console.log(merge);


var paths = [];
for(var cursor = 0; cursor < text.length; cursor++){
	paths[cursor] = './crashfont/crashfont_' + (crashfontString.indexOf(text.charAt(cursor))+1).toString() + '.png'
	
}

merge(paths)
  .then((img) => {
    // Save image as file
    img.write('out.png', () => console.log('done'));
 
    // Get image as `Buffer`
    //img.getBuffer(img.getMIME(), (buf) => console.log(buf));
  });*/


	
	

